# Practical Lab 1 workspace

Open `DispatchAudit/DispatchAudit.sln` in Visual Studio. The assessed source files are intentionally blank except for a comment. Write your own program from the brief; there is no completed implementation or fill-in algorithm.

Use C++17, Debug, x64 and warning level /W4. The supplied configuration targets the Visual Studio v143 C++ toolset and a Windows 10/11 SDK. Retarget to an installed compatible C++ toolset if needed. Environment setup is not a new assessed programming concept. Each solution has one executable project and should contain one main function.

Keep the whole Lab_01 folder as your repository root. Do not move individual files between the two lab workspaces. Record tests and explanations in evidence.md. Commit and push to your authorised private repository. Do not publish solutions publicly.

The supplied project is unimplemented, not a passing starter program. No copied tutorial code or model solution is included.
